name: Review Stack
# review-stack-control: refresh marker for reusable workflow and action updates

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  review-stack:
    uses: madmoneymike5/review-stack-control/.github/workflows/review-stack.yml@v1
    with:
      native-command: >-
        corepack enable && pnpm install --frozen-lockfile --ignore-scripts && pnpm run test && pnpm run build
      central-ref: v1
